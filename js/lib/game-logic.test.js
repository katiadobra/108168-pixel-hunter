import assert from 'assert';
import {setAnswer, checkAnswers, checkFinished, createListener} from './game-logic';

describe('Game parameters', () => {
  describe('Settings', () => {
    it('setAnswer', () => {
      assert.equel();
    });

    it('checkAnswers', () => {
      assert.equel();
    });

    it('checkFinished', () => {
      assert.equel();
    });

    it('createListener', () => {
      assert.equel();
    });
  });
});
